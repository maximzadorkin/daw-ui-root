import { makeStyles } from '@quarx-ui/core';

const useStyles = makeStyles(
    {
        root: {
            height: '100%',
            overflowY: 'auto',
            position: 'relative',
        },
    },
    { name: 'PageBody' },
);

export { useStyles };
