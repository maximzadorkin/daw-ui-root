import { makeStyles } from '@quarx-ui/core';

const useStyles = makeStyles(
    {
        root: {
            display: 'flex',
            flexDirection: 'column',
            height: '100vh',
        },
    },
    { name: 'Page' },
);

export { useStyles };
