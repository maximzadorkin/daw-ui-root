interface LoaderProps {
    /** [x, y, z]
     * @default [0, 0, 0] */
    position?: [number, number, number];

    /** width/height
     * @default 24 */
    size?: number;
}

export type { LoaderProps };
