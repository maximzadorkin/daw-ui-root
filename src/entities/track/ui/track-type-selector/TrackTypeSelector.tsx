import React, { FC, Fragment } from 'react';
import { Button } from '@quarx-ui/core';

const TrackTypeSelector: FC = () => {
    return (
        <Fragment>
            <Button></Button>
        </Fragment>
    );
};

export { TrackTypeSelector };
